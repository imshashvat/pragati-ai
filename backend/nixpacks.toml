# nixpacks.toml — tells Railway how to build the backend
[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "libstdcxx5"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
